/**
 * Parses books and subtitles
 */

/**
 * Parses epubs
 * @param bookfile the epub file in the format of the express bodyparser
 * @param req request object from express
 * @param res response object from express
 * @param processingsequence all files in the processing chain
 */
exports.parseBook = function(bookfile, req, res, processingsequence){

}

/**
 * Parses subtitles
 * @param subtitlefile the srt file in the format of the express bodyparser
 * @param req request object from express
 * @param res response object from express
 * @param processingsequence all files in the processing chain
 */
exports.parseSubtitle = function(subtitlefile, req, res, processingsequence){

}
